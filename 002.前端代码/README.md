# 前端项目

本目录包含医疗智能体管理平台的完整前端应用。

```bash
npm install
npm run dev
```

- 前端地址：`http://localhost:3005`
- 后端 API：开发环境通过 Vite 将 `/api` 代理到 `http://localhost:3000`
- 生产构建：`npm run build`

在本目录运行 `npm run dev:all` 可以同时启动 `002.前端代码` 和 `004.后端代码`。
